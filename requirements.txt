# --- Orchestration ---
langgraph>=1.0,<2.0

# --- OCR ---
pytesseract==0.3.13
Pillow==10.4.0

# --- LLM ---
openai==1.51.0                    # used for OCR field extraction (or Groq via OpenAI-compatible client)
httpx<0.28                        # pinned: newer httpx breaks openai==1.51.0's client init
google-genai==2.22.0               # Gemini embeddings for policy search (free tier)

# --- Vector DB ---
pinecone==6.0.0

# --- Database ---
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9           # PostgreSQL driver

# --- Policy document parsing ---
python-docx==1.1.2

# --- Notifications (used only when APP_ENV=production) ---
sendgrid==6.11.0
twilio==9.3.4

# --- Validation / config ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Frontend ---
streamlit==1.38.0

# --- Testing ---
pytest==8.3.3