# --- Orchestration ---
langgraph>=1.0,<2.0

# --- OCR ---
pytesseract==0.3.13
Pillow==10.4.0

# --- LLM ---
openai>=1.55                      # newer version fixes the httpx-conflict bug from 1.51.0
google-genai>=2.22.0               # Gemini embeddings for policy search (free tier)

# --- Vector DB ---
pinecone==6.0.0

# --- Database ---
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9           # PostgreSQL driver (used by SQLAlchemy)
psycopg[binary]>=3.1              # PostgreSQL driver v3 (used by langgraph-checkpoint-postgres)
langgraph-checkpoint-postgres>=2.0   # persistent LangGraph checkpointer (app/db/checkpointer.py)

# --- Policy document parsing ---
python-docx==1.1.2

# --- Notifications (used only when APP_ENV=production) ---
sendgrid==6.11.0
twilio==9.3.4

# --- Validation / config ---
pydantic>=2.12.5
pydantic-settings>=2.7
python-dotenv==1.0.1

# --- Frontend ---
streamlit==1.38.0

# --- Testing ---
pytest==8.3.3